apiVersion: reddiagent.dev/v0.2
kind: Agent
metadata:
  name: invalid-string-instructions
  description: Uses the legacy bare-string instruction path shape.
model:
  capability: chat
  providers:
    preferred: openai
  requirements:
    toolCalling: false
    structuredOutput: true
harness:
  instructions: ./prompts/system.md
  runtime:
    target: local-python
extensions: {}
