apiVersion: reddiagent.dev/v0.1
kind: Agent
metadata:
  name: bad-data-source
  description: Invalid example for data source guidance.
model:
  capability: chat
  providers:
    preferred: openai:gpt-4.1-mini
  requirements: {}
harness:
  instructions:
    inline: Summarize approved sources.
  dataSources:
    - id: docs
      description: Approved docs without a source type.
  runtime:
    target: local-python
