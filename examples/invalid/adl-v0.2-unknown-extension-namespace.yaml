apiVersion: reddiagent.dev/v0.2
kind: Agent
metadata:
  name: unknown-extension-namespace
  description: Invalid unprefixed extension namespace in strict ADL v0.2.
model:
  capability: chat
  providers:
    preferred: openai
  requirements:
    toolCalling: true
harness:
  instructions:
    inline: "Stay static."
  runtime:
    target: local-python
extensions:
  payments:
    enabled: true
