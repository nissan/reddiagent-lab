apiVersion: reddiagent.dev/v0.2
kind: Agent
metadata:
  name: unknown-provider-id
  description: Invalid ADL v0.2 fixture for fail-closed provider identifiers.
model:
  capability: chat
  providers:
    preferred: openai:gpt-4.1-mini
  requirements:
    toolCalling: false
harness:
  instructions:
    inline: "This fixture must fail before provider compatibility reporting."
  runtime:
    target: local-python
extensions: {}
