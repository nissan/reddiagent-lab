apiVersion: reddiagent.dev/v0.2
kind: Agent
metadata:
  name: invalid-requested-level
  description: Invalid ADL v0.2 conformance request outside the supported level range.
model:
  capability: chat
  providers:
    preferred: openai
  requirements:
    toolCalling: false
    structuredOutput: true
conformance:
  requestedLevel: 5
harness:
  instructions:
    inline: "Do not execute."
  runtime:
    target: local-python
extensions: {}
